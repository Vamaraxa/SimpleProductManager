<nav class="navbar navbar-expand-lg navbar-dark bg-dark text-light">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 p-0">
                <li class="nav-item dropdown fw-bold">
                    <a class="nav-link {% if app.request.get('_route') == 'index' %}active{% endif %}" href="{{ path('index') }}">Index</a>
                </li>
                {% if app.user %}
                <li class="nav-item dropdown fw-bold">
                    <a class="nav-link {% if app.request.get('_route') == 'products.manage' %}active{% endif %}" href="{{ path('products.manage') }}">Manage</a>
                </li>
                {% endif %}
            </ul>
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 p-0">
                <li class="d-flex align-items-center">
                    {% if app.user %}
                        <span class="nav-link">{{ app.user.username }}</span>
                        <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                    {% else %}
                        <a class="nav-link" href="{{ path('app_login') }}">Log In</a>
                        <a class="nav-link" href="{{ path('app_register') }}">Register</a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>